schema {
    query: Query,
    mutation: Mutation
}

type Query {
    getProducts(pageNum: Int, pageSize: Int): [Product!]!
    getSeller(id: ID!): Seller!
    getSellers(pageNum: Int, pageSize: Int): [Seller!]!
}

type Mutation {
    purchaseProduct(id: ID!, quantity: Int): Product!
}